#ifndef INVENTAIRE_H  // Vérifie si le symbole INVENTAIRE_H n'est pas déjà défini
#define INVENTAIRE_H  // Définit le symbole INVENTAIRE_H pour éviter une double inclusion

// Déclaration de la fonction afficherQuartilesAge()
void afficherQuartilesAge();  // Cette fonction sert à afficher l'inventaire des animaux par tranche d'âge

// Déclaration de la fonction inventaireAge() si elle existe
void inventaireAge();  // Déclare la fonction inventaireAge() si elle est utilisée dans ton programme

#endif  // Fin de la condition pour inclure ce fichier uniquement une fois
