#ifndef COMPARER_H  // Vérifie si COMPARER_H n'a pas déjà été défini
#define COMPARER_H  // Si ce n'est pas le cas, définit COMPARER_H pour éviter une inclusion multiple

// Déclaration de la fonction comparer qui compare deux chaînes de caractères insensibles à la casse
int comparer(const char *s1, const char *s2);  // Compare les deux chaînes et retourne 1 si elles sont égales, 0 sinon

#endif  // Fin de la protection d'inclusion
