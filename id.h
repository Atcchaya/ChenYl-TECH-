#ifndef ID_H  // Si ID_H n'a pas encore été défini
#define ID_H  // Définit ID_H pour éviter que le fichier soit inclus plusieurs fois

// Déclaration de la fonction genererID. Cette fonction génère un ID unique et le retourne.
int genererID();  

#endif  // Fin du bloc conditionnel pour l'inclusion du fichier d'en-tête
